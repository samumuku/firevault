# firevault

Personal project for saving passwords. Used as an extension for chromium based browsers.
For personal use

# Versions

- Python : **`3.14.2`**

# Developer mode

### Installing the dependencies

In the **`src`** folder

```bash
pip install -r requirements.txt
```

### Launch the app

In the **`src`** folder

```bash
python main.py
```
